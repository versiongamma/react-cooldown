import useCooldown from "./useCooldown";
import frequencies, { getMillisecondsFromFrequency } from "./frequencies";

export { frequencies as FREQUENCIES, getMillisecondsFromFrequency };
export default useCooldown;
